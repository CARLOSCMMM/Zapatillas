<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ZapatillasApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Zapatillas">

        <activity
            android:name=".presentacion.ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".presentacion.ui.login.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".presentacion.ui.addEdit.AddEditZapatillaActivity"
            android:exported="false" />
            
        <activity
            android:name=".presentacion.ui.detail.ZapatillaDetailActivity"
            android:exported="false" />

        <activity
            android:name="com.example.zapatillas.presentacion.ui.main.MainActivity"
            android:exported="false">
        </activity>

    </application>

</manifest>
